# Imported from the reference prompts repository (playbooks/CUSTOM_EMAIL_PASSWORD_AUTH.md).
# Regenerate with `pnpm --filter @power/knowledge run import <source>` — do not hand-edit;
# hand-authored changes belong in a new pack that `deprecates` this one.
name: pb_custom_email_password_auth
title: "Custom Email/Password JWT Authentication (FastAPI + React + MongoDB)"
version: 1
category: integration
owner: prompts-repo
summary: "Integration guidance for Custom Email/Password JWT Authentication (FastAPI + React + MongoDB), imported from the reference playbook corpus."
matching_criteria:
  - "the task implements user sign-in, sign-up, or session handling"
  - "the task touches authentication tokens, cookies, or an OAuth flow"
  - "the task sends transactional or bulk email"
  - "the task uploads, stores, or serves user files or media"
  - "the task integrates Custom Email/Password JWT Authentication (FastAPI + React + MongoDB)"
tags: [imported]
content: imported/pb_custom_email_password_auth.md
