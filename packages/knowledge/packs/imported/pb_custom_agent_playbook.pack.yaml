# Imported from the reference prompts repository (playbooks/CUSTOM_AGENT_PLAYBOOK.md).
# Regenerate with `pnpm --filter @power/knowledge run import <source>` — do not hand-edit;
# hand-authored changes belong in a new pack that `deprecates` this one.
name: pb_custom_agent_playbook
title: "the platform Custom Agent API Integration Playbook"
version: 1
category: integration
owner: prompts-repo
enabled: false
summary: "the platform Custom Agent API Integration Playbook. DISABLED ON IMPORT: this playbook instructs the agent to use source-platform services (its auth broker, credit system, or asset host) that do not exist outside that platform. Adapt it before enabling."
matching_criteria:
  - "the task calls a large language model API"
  - "the task builds chat, completion, or conversation features"
  - "the task integrates the platform Custom Agent API Integration Playbook"
anti_criteria:
  - "always — this pack is disabled until its platform-specific instructions are adapted"
tags: [imported]
content: imported/pb_custom_agent_playbook.md
