# Imported from the reference prompts repository (playbooks/AUTH_CROSS_PLATFORM.md).
# Regenerate with `pnpm --filter @power/knowledge run import <source>` — do not hand-edit;
# hand-authored changes belong in a new pack that `deprecates` this one.
name: pb_auth_cross_platform
title: "the platform Managed Google Auth Login Playbook for cross-platform apps (shared backend + web React and Expo/React Native mobile frontends)"
version: 1
category: integration
owner: prompts-repo
enabled: false
summary: "the platform Managed Google Auth Login Playbook for cross-platform apps (shared backend + web React and Expo/React Native mobile frontends). DISABLED ON IMPORT: this playbook instructs the agent to use source-platform services (its auth broker, credit system, or asset host) that do not exist outside that platform. Adapt it before enabling."
matching_criteria:
  - "the task implements user sign-in, sign-up, or session handling"
  - "the task touches authentication tokens, cookies, or an OAuth flow"
  - "the task sends transactional or bulk email"
  - "the task shows maps, geocodes addresses, or works with place data"
  - "the task uploads, stores, or serves user files or media"
  - "the task integrates the platform Managed Google Auth Login Playbook for cross-platform apps (shared backend + web React and Expo/React Native mobile frontends)"
anti_criteria:
  - "always — this pack is disabled until its platform-specific instructions are adapted"
tags: [imported]
content: imported/pb_auth_cross_platform.md
