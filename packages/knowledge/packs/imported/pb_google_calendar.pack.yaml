# Imported from the reference prompts repository (playbooks/GOOGLE_CALENDAR.md).
# Regenerate with `pnpm --filter @power/knowledge run import <source>` — do not hand-edit;
# hand-authored changes belong in a new pack that `deprecates` this one.
name: pb_google_calendar
title: "Google Calendar"
version: 1
category: integration
owner: prompts-repo
enabled: false
summary: "Google Calendar. DISABLED ON IMPORT: this playbook instructs the agent to use source-platform services (its auth broker, credit system, or asset host) that do not exist outside that platform. Adapt it before enabling."
matching_criteria:
  - "the task implements user sign-in, sign-up, or session handling"
  - "the task touches authentication tokens, cookies, or an OAuth flow"
  - "the task sends transactional or bulk email"
  - "the task manages calendars, bookings, or scheduled appointments"
  - "the task builds search, retrieval, or embedding-backed features"
  - "the task integrates Google Calendar"
anti_criteria:
  - "always — this pack is disabled until its platform-specific instructions are adapted"
tags: [imported]
content: imported/pb_google_calendar.md
