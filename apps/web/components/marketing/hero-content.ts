/**
 * The hero's three phrases.
 *
 * Shared between the server-rendered composition and the client animation so the
 * two can never disagree — the words a crawler reads are the words that ride the
 * path.
 */
export const PHRASES = ['You describe.', 'Agents build.', 'Gates decide.'] as const;
